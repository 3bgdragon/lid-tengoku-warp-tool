# M2G 적용 순서 호환성 검증 — 2026-09-11

대상: Steam build 25136512, M2G Node.js v1.1.0, 워프툴 1.1.1-dev.

제보된 `F0439B6D568FF02E0E9488F4ED6323056C84F842`는 순정 BrgGame.upk에 M2G v1.1.0만 적용한 결과와 일치했다. 전체 파일 해시 허용 목록에 없어서 발생한 차단이었다. 파일 해시 비교만 생략하는 대신 알려진 M2G 패키지를 정확한 원본으로 재구성한 뒤 워프를 변경하고 M2G를 다시 생성한다.

## 자동 테스트

`npm test`: 기존 6개 + 신규 4개. 제보 해시, 8개 M2G 파일 상태, 지원되지 않는 입력 차단, UI 상태 식별을 확인한다.

## 게임 복사본 통합 검증

2026-09-11 결과: **4개 조합 모두 통과**. 양쪽 적용 순서의 네 파일 해시 일치, 워프 제거 시 M2G 유지, 전체 백업 복원 일치를 확인했다. 단위 테스트도 10개 모두 통과했다.

그로기 OFF/ON × 근접 방어 제한 OFF/ON = 4개 조합 각각에서 다음을 검사한다.

1. M2G → 워프 적용 결과를 확인.
2. 워프 → M2G 적용 결과와 네 파일의 SHA-1이 모두 동일한지 확인.
3. 워프 제거 후 나이프 모드만 남은 상태로 네 파일이 정확히 복귀하는지 확인.
4. 워프 도구 전체 백업 복원 후 직전 두 모드 적용 상태로 정확히 복귀하는지 확인.
5. 원본 입력 EXE·맵 파일이 변경되지 않았는지 확인.

검증은 새로 만든 `.integration-temp/m2g-order-*` 폴더 안에서만 수행한다. 사용자의 실제 게임·세이브·MASTER DB는 수정하지 않는다. 테스트가 만든 임시 백업만 각 조합 검증 후 정리하며, 결과 JSON과 마지막 게임 복사본은 남긴다.

개발용 재현 명령:

```text
node tests/run-m2g-integration.js "워프 적용 EXE 복사본" "워프 적용 맵 폴더" "열거된 BrgGame 원본 조합 폴더"
```

`compat/m2g/profiles.json`의 8개 입력·출력은 실제 생성 결과를 SHA-1/SHA-256으로 고정했다. M2G를 분리할 때 원본 크기와 두 압축 청크 디렉터리 항목만 복구하고, 원본 SHA-256이 맞지 않으면 중단한다. 다시 생성한 M2G 파일도 알려진 SHA-256과 일치해야 한다.

이번 검증은 파일 생성·적용·제거·전체 백업 복원 검증이다. 이 변경 이후 실게임에서 새로 플레이했다고 주장하지 않는다. 기존 새 Steam 빌드의 게임 내 층 이동 검증 상태는 바꾸지 않았다. M2G 도구 자체의 전체 백업 복원은 선택적 제거가 아니며 기존 외부 변경 차단 규칙을 유지한다.
